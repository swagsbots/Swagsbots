services:
  - type: web
    name: swagshots-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bitget_trading_bot.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
